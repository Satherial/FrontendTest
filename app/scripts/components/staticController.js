define([],
  function() {
    'use strict';

    function homeController($scope, $location, server) {
    }
    homeController.$inject=['$scope', '$location', 'server'];
    return homeController;
});
